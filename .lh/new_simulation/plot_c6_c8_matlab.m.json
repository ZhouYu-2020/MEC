{
    "sourceFile": "new_simulation/plot_c6_c8_matlab.m",
    "activeCommit": 0,
    "commits": [
        {
            "activePatchIndex": 3,
            "patches": [
                {
                    "date": 1775015621931,
                    "content": "Index: \n===================================================================\n--- \n+++ \n"
                },
                {
                    "date": 1775015939541,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -2,11 +2,14 @@\n % Plot C6-C8 violation rates for two stress dimensions:\r\n % 1) scenario = user_num\r\n % 2) scenario = f_scale\r\n % Input file expected: new_simulation/results_paper_c5/stress_scan_summary.csv\r\n+% plotMode options: 'line' (default) or 'groupedbar'\r\n \r\n clear; clc; close all;\r\n \r\n+plotMode = 'line';\r\n+\r\n %% Paths\r\n thisFileDir = fileparts(mfilename('fullpath'));\r\n inputCsv = fullfile(thisFileDir, 'results_paper_c5', 'stress_scan_summary.csv');\r\n outDir = fullfile(thisFileDir, 'figures_paper_c5');\r\n@@ -18,27 +21,44 @@\n     mkdir(outDir);\r\n end\r\n \r\n %% Load and sanitize table\r\n-T = readtable(inputCsv, 'TextType', 'string');\r\n+T = readtable(inputCsv, 'TextType', 'string', 'VariableNamingRule', 'preserve');\r\n \r\n-requiredCols = [\"scenario\",\"value\",\"policy\", ...\r\n-    \"p_c6_viol_mean\",\"p_c7_viol_mean\",\"p_c8_viol_mean\"];\r\n-for i = 1:numel(requiredCols)\r\n-    if ~ismember(requiredCols(i), T.Properties.VariableNames)\r\n-        error('Missing required column: %s', requiredCols(i));\r\n-    end\r\n-end\r\n+vRaw = string(T.Properties.VariableNames);\r\n+vNorm = lower(strtrim(vRaw));\r\n+vNorm = regexprep(vNorm, '^[\\x{FEFF}\\xEF\\xBB\\xBF]+', '');\r\n \r\n-% Convert numeric-like columns safely\r\n-numCols = [\"value\",\"p_c6_viol_mean\",\"p_c7_viol_mean\",\"p_c8_viol_mean\"];\r\n-for i = 1:numel(numCols)\r\n-    c = numCols(i);\r\n-    if ~isnumeric(T.(c))\r\n-        T.(c) = str2double(string(T.(c)));\r\n-    end\r\n+findCol = @(name) find(vNorm == lower(name), 1, 'first');\r\n+\r\n+iScenario = findCol(\"scenario\");\r\n+iValue = findCol(\"value\");\r\n+iPolicy = findCol(\"policy\");\r\n+iC6 = findCol(\"p_c6_viol_mean\");\r\n+iC7 = findCol(\"p_c7_viol_mean\");\r\n+iC8 = findCol(\"p_c8_viol_mean\");\r\n+\r\n+if isempty(iScenario) || isempty(iValue) || isempty(iPolicy) || ...\r\n+   isempty(iC6) || isempty(iC7) || isempty(iC8)\r\n+    error(['Missing required columns. Found columns are: ', ...\r\n+        strjoin(cellstr(vRaw), ', ')]);\r\n end\r\n \r\n+cScenario = char(vRaw(iScenario));\r\n+cValue = char(vRaw(iValue));\r\n+cPolicy = char(vRaw(iPolicy));\r\n+cC6 = char(vRaw(iC6));\r\n+cC7 = char(vRaw(iC7));\r\n+cC8 = char(vRaw(iC8));\r\n+\r\n+% Build canonical columns\r\n+T.scenario = string(T.(cScenario));\r\n+T.policy = string(T.(cPolicy));\r\n+T.value = str2double(string(T.(cValue)));\r\n+T.p_c6_viol_mean = str2double(string(T.(cC6)));\r\n+T.p_c7_viol_mean = str2double(string(T.(cC7)));\r\n+T.p_c8_viol_mean = str2double(string(T.(cC8)));\r\n+\r\n % Drop rows with missing key values\r\n valid = ~ismissing(T.scenario) & ~ismissing(T.policy) & ...\r\n         ~isnan(T.value) & ~isnan(T.p_c6_viol_mean) & ...\r\n         ~isnan(T.p_c7_viol_mean) & ~isnan(T.p_c8_viol_mean);\r\n@@ -56,9 +76,13 @@\n %% Plot helper settings\r\n metricNames = {'p_c6','p_c7','p_c8'};\r\n metricLabels = {'C6 violation rate','C7 violation rate','C8 violation rate'};\r\n scenarioList = {'user_num','f_scale'};\r\n-outNames = {'c6_c8_vs_user_num.png','c6_c8_vs_f_scale.png'};\r\n+if strcmpi(plotMode, 'groupedbar')\r\n+    outNames = {'c6_c8_vs_user_num_groupedbar.png','c6_c8_vs_f_scale_groupedbar.png'};\r\n+else\r\n+    outNames = {'c6_c8_vs_user_num.png','c6_c8_vs_f_scale.png'};\r\n+end\r\n \r\n for s = 1:numel(scenarioList)\r\n     sc = scenarioList{s};\r\n     S = G(G.scenario == sc, :);\r\n@@ -86,20 +110,39 @@\n     for m = 1:3\r\n         nexttile; hold on; grid on; box on;\r\n         yField = metricNames{m};\r\n \r\n-        for p = 1:nP\r\n-            pol = policies(p);\r\n-            P = S(S.policy == pol, :);\r\n-            [~, ord] = sort(P.value);\r\n-            Px = P.value(ord);\r\n-            Py = P.(yField)(ord);\r\n+        if strcmpi(plotMode, 'groupedbar')\r\n+            Y = nan(numel(xVals), nP);\r\n+            for p = 1:nP\r\n+                pol = policies(p);\r\n+                P = S(S.policy == pol, :);\r\n+                for xi = 1:numel(xVals)\r\n+                    r = P(P.value == xVals(xi), :);\r\n+                    if ~isempty(r)\r\n+                        Y(xi, p) = r.(yField)(1);\r\n+                    end\r\n+                end\r\n+            end\r\n+            b = bar(xVals, Y, 'grouped', 'LineStyle', 'none');\r\n+            for p = 1:min(nP, numel(b))\r\n+                b(p).FaceColor = cmap(p,:);\r\n+                b(p).DisplayName = char(policies(p));\r\n+            end\r\n+        else\r\n+            for p = 1:nP\r\n+                pol = policies(p);\r\n+                P = S(S.policy == pol, :);\r\n+                [~, ord] = sort(P.value);\r\n+                Px = P.value(ord);\r\n+                Py = P.(yField)(ord);\r\n \r\n-            plot(Px, Py, 'LineWidth', 1.8, ...\r\n-                'Color', cmap(p,:), ...\r\n-                'Marker', mk{mod(p-1, numel(mk))+1}, ...\r\n-                'MarkerSize', 6, ...\r\n-                'DisplayName', char(pol));\r\n+                plot(Px, Py, 'LineWidth', 1.8, ...\r\n+                    'Color', cmap(p,:), ...\r\n+                    'Marker', mk{mod(p-1, numel(mk))+1}, ...\r\n+                    'MarkerSize', 6, ...\r\n+                    'DisplayName', char(pol));\r\n+            end\r\n         end\r\n \r\n         xlabel(sc, 'FontSize', 11);\r\n         ylabel(metricLabels{m}, 'FontSize', 11);\r\n"
                },
                {
                    "date": 1775016670170,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -20,14 +20,39 @@\n if ~isfolder(outDir)\r\n     mkdir(outDir);\r\n end\r\n \r\n-%% Load and sanitize table\r\n-T = readtable(inputCsv, 'TextType', 'string', 'VariableNamingRule', 'preserve');\r\n+%% Load and sanitize table (robust across MATLAB versions)\r\n+fid = fopen(inputCsv, 'r');\r\n+if fid < 0\r\n+    error('Cannot open CSV file: %s', inputCsv);\r\n+end\r\n+headerLine = fgetl(fid);\r\n+fclose(fid);\r\n \r\n-vRaw = string(T.Properties.VariableNames);\r\n+if ~ischar(headerLine) && ~isstring(headerLine)\r\n+    error('CSV header is empty or unreadable: %s', inputCsv);\r\n+end\r\n+\r\n+headerLine = char(headerLine);\r\n+headerLine = regexprep(headerLine, '^[\\x{FEFF}\\xEF\\xBB\\xBF]+', '');\r\n+rawNames = strsplit(headerLine, ',');\r\n+rawNames = strtrim(rawNames);\r\n+\r\n+% Use stable temporary variable names to avoid namelengthmax truncation issues\r\n+tmpNames = arrayfun(@(k) sprintf('c%d', k), 1:numel(rawNames), 'UniformOutput', false);\r\n+opts = delimitedTextImportOptions('Delimiter', ',', ...\r\n+    'DataLines', [2 inf], ...\r\n+    'ReadVariableNames', false, ...\r\n+    'ConsecutiveDelimitersRule', 'split', ...\r\n+    'ExtraColumnsRule', 'ignore', ...\r\n+    'EmptyLineRule', 'read');\r\n+opts.VariableNames = tmpNames;\r\n+opts.VariableTypes = repmat({'string'}, 1, numel(tmpNames));\r\n+T = readtable(inputCsv, opts);\r\n+\r\n+vRaw = string(rawNames);\r\n vNorm = lower(strtrim(vRaw));\r\n-vNorm = regexprep(vNorm, '^[\\x{FEFF}\\xEF\\xBB\\xBF]+', '');\r\n \r\n findCol = @(name) find(vNorm == lower(name), 1, 'first');\r\n \r\n iScenario = findCol(\"scenario\");\r\n@@ -38,27 +63,20 @@\n iC8 = findCol(\"p_c8_viol_mean\");\r\n \r\n if isempty(iScenario) || isempty(iValue) || isempty(iPolicy) || ...\r\n    isempty(iC6) || isempty(iC7) || isempty(iC8)\r\n-    error(['Missing required columns. Found columns are: ', ...\r\n+    error(['Missing required columns. Found header columns are: ', ...\r\n         strjoin(cellstr(vRaw), ', ')]);\r\n end\r\n \r\n-cScenario = char(vRaw(iScenario));\r\n-cValue = char(vRaw(iValue));\r\n-cPolicy = char(vRaw(iPolicy));\r\n-cC6 = char(vRaw(iC6));\r\n-cC7 = char(vRaw(iC7));\r\n-cC8 = char(vRaw(iC8));\r\n+% Build canonical columns from temporary import names\r\n+T.scenario = string(T.(tmpNames{iScenario}));\r\n+T.policy = string(T.(tmpNames{iPolicy}));\r\n+T.value = str2double(string(T.(tmpNames{iValue})));\r\n+T.p_c6_viol_mean = str2double(string(T.(tmpNames{iC6})));\r\n+T.p_c7_viol_mean = str2double(string(T.(tmpNames{iC7})));\r\n+T.p_c8_viol_mean = str2double(string(T.(tmpNames{iC8})));\r\n \r\n-% Build canonical columns\r\n-T.scenario = string(T.(cScenario));\r\n-T.policy = string(T.(cPolicy));\r\n-T.value = str2double(string(T.(cValue)));\r\n-T.p_c6_viol_mean = str2double(string(T.(cC6)));\r\n-T.p_c7_viol_mean = str2double(string(T.(cC7)));\r\n-T.p_c8_viol_mean = str2double(string(T.(cC8)));\r\n-\r\n % Drop rows with missing key values\r\n valid = ~ismissing(T.scenario) & ~ismissing(T.policy) & ...\r\n         ~isnan(T.value) & ~isnan(T.p_c6_viol_mean) & ...\r\n         ~isnan(T.p_c7_viol_mean) & ~isnan(T.p_c8_viol_mean);\r\n"
                },
                {
                    "date": 1775205571274,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -1,180 +1,180 @@\n-%% plot_c6_c8_matlab.m\r\n-% Plot C6-C8 violation rates for two stress dimensions:\r\n-% 1) scenario = user_num\r\n-% 2) scenario = f_scale\r\n-% Input file expected: new_simulation/results_paper_c5/stress_scan_summary.csv\r\n-% plotMode options: 'line' (default) or 'groupedbar'\r\n+% 文件路径（请根据实际情况修改）\r\n+csvFile = 'results_paper_c5/stress_scan_summary.csv';\r\n+outDir = 'figures_paper_c5';\r\n \r\n-clear; clc; close all;\r\n+% ==================== 检测分隔符 ====================\r\n+fid = fopen(csvFile, 'r', 'n', 'UTF-8');\r\n+if fid == -1\r\n+    error('无法打开文件: %s', csvFile);\r\n+end\r\n+firstLine = fgetl(fid);\r\n+fclose(fid);\r\n \r\n-plotMode = 'line';\r\n+% 去除 BOM\r\n+if length(firstLine) >= 1 && double(firstLine(1)) == 65279\r\n+    firstLine = firstLine(2:end);\r\n+end\r\n \r\n-%% Paths\r\n-thisFileDir = fileparts(mfilename('fullpath'));\r\n-inputCsv = fullfile(thisFileDir, 'results_paper_c5', 'stress_scan_summary.csv');\r\n-outDir = fullfile(thisFileDir, 'figures_paper_c5');\r\n+% 自动检测分隔符\r\n+delimiters = {',', '\\t', ' ', ';', '|'};\r\n+delim = '';\r\n+maxCols = 1;\r\n+for i = 1:length(delimiters)\r\n+    parts = strsplit(firstLine, delimiters{i});\r\n+    if length(parts) > maxCols\r\n+        maxCols = length(parts);\r\n+        delim = delimiters{i};\r\n+    end\r\n+end\r\n+if isempty(delim)\r\n+    error('未检测到分隔符');\r\n+end\r\n+fprintf('使用分隔符: \"%s\"\\n', delim);\r\n \r\n-if ~isfile(inputCsv)\r\n-    error('Input CSV not found: %s', inputCsv);\r\n+% ==================== 读取全部数据 ====================\r\n+fid = fopen(csvFile, 'r', 'n', 'UTF-8');\r\n+if fid == -1\r\n+    error('无法打开文件: %s', csvFile);\r\n end\r\n-if ~isfolder(outDir)\r\n-    mkdir(outDir);\r\n+headerLine = fgetl(fid);\r\n+if double(headerLine(1)) == 65279\r\n+    headerLine = headerLine(2:end);\r\n end\r\n+headers = strsplit(headerLine, delim);\r\n+headers = strtrim(headers);\r\n+nCols = length(headers);\r\n \r\n-%% Load and sanitize table (robust across MATLAB versions)\r\n-fid = fopen(inputCsv, 'r');\r\n-if fid < 0\r\n-    error('Cannot open CSV file: %s', inputCsv);\r\n+dataLines = {};\r\n+rowCount = 0;\r\n+while ~feof(fid)\r\n+    line = fgetl(fid);\r\n+    rowCount = rowCount + 1;\r\n+    if isempty(strtrim(line))\r\n+        continue;\r\n+    end\r\n+    parts = strsplit(line, delim);\r\n+    if length(parts) ~= nCols\r\n+        warning('行 %d 列数 (%d) 与表头 (%d) 不一致，跳过', rowCount+1, length(parts), nCols);\r\n+        continue;\r\n+    end\r\n+    dataLines = [dataLines; parts]; %#ok<AGROW>\r\n end\r\n-headerLine = fgetl(fid);\r\n fclose(fid);\r\n \r\n-if ~ischar(headerLine) && ~isstring(headerLine)\r\n-    error('CSV header is empty or unreadable: %s', inputCsv);\r\n+% 转换为表格\r\n+dataTable = table();\r\n+for c = 1:nCols\r\n+    colName = headers{c};\r\n+    colData = dataLines(:, c);\r\n+    numData = str2double(colData);\r\n+    if all(~isnan(numData))\r\n+        dataTable.(colName) = numData;\r\n+    else\r\n+        dataTable.(colName) = colData;\r\n+    end\r\n end\r\n \r\n-headerLine = char(headerLine);\r\n-headerLine = regexprep(headerLine, '^[\\x{FEFF}\\xEF\\xBB\\xBF]+', '');\r\n-rawNames = strsplit(headerLine, ',');\r\n-rawNames = strtrim(rawNames);\r\n+% 所需列名（小写）\r\n+requiredCols = {'scenario', 'value', 'policy', 'p_c6_viol_mean', ...\r\n+                'p_c7_viol_mean', 'p_c8_viol_mean'};\r\n \r\n-% Use stable temporary variable names to avoid namelengthmax truncation issues\r\n-tmpNames = arrayfun(@(k) sprintf('c%d', k), 1:numel(rawNames), 'UniformOutput', false);\r\n-opts = delimitedTextImportOptions('Delimiter', ',', ...\r\n-    'DataLines', [2 inf], ...\r\n-    'ReadVariableNames', false, ...\r\n-    'ConsecutiveDelimitersRule', 'split', ...\r\n-    'ExtraColumnsRule', 'ignore', ...\r\n-    'EmptyLineRule', 'read');\r\n-opts.VariableNames = tmpNames;\r\n-opts.VariableTypes = repmat({'string'}, 1, numel(tmpNames));\r\n-T = readtable(inputCsv, opts);\r\n-\r\n-vRaw = string(rawNames);\r\n-vNorm = lower(strtrim(vRaw));\r\n-\r\n-findCol = @(name) find(vNorm == lower(name), 1, 'first');\r\n-\r\n-iScenario = findCol(\"scenario\");\r\n-iValue = findCol(\"value\");\r\n-iPolicy = findCol(\"policy\");\r\n-iC6 = findCol(\"p_c6_viol_mean\");\r\n-iC7 = findCol(\"p_c7_viol_mean\");\r\n-iC8 = findCol(\"p_c8_viol_mean\");\r\n-\r\n-if isempty(iScenario) || isempty(iValue) || isempty(iPolicy) || ...\r\n-   isempty(iC6) || isempty(iC7) || isempty(iC8)\r\n-    error(['Missing required columns. Found header columns are: ', ...\r\n-        strjoin(cellstr(vRaw), ', ')]);\r\n+% 列名映射（忽略大小写）\r\n+actualCols = dataTable.Properties.VariableNames;\r\n+colMap = containers.Map();\r\n+for i = 1:length(actualCols)\r\n+    key = lower(strtrim(actualCols{i}));\r\n+    colMap(key) = actualCols{i};\r\n end\r\n \r\n-% Build canonical columns from temporary import names\r\n-T.scenario = string(T.(tmpNames{iScenario}));\r\n-T.policy = string(T.(tmpNames{iPolicy}));\r\n-T.value = str2double(string(T.(tmpNames{iValue})));\r\n-T.p_c6_viol_mean = str2double(string(T.(tmpNames{iC6})));\r\n-T.p_c7_viol_mean = str2double(string(T.(tmpNames{iC7})));\r\n-T.p_c8_viol_mean = str2double(string(T.(tmpNames{iC8})));\r\n-\r\n-% Drop rows with missing key values\r\n-valid = ~ismissing(T.scenario) & ~ismissing(T.policy) & ...\r\n-        ~isnan(T.value) & ~isnan(T.p_c6_viol_mean) & ...\r\n-        ~isnan(T.p_c7_viol_mean) & ~isnan(T.p_c8_viol_mean);\r\n-T = T(valid, :);\r\n-\r\n-%% Aggregate duplicates by (scenario, value, policy)\r\n-G = groupsummary(T, {'scenario','value','policy'}, 'mean', ...\r\n-    {'p_c6_viol_mean','p_c7_viol_mean','p_c8_viol_mean'});\r\n-\r\n-% Rename grouped columns for easier access\r\n-G.Properties.VariableNames{'mean_p_c6_viol_mean'} = 'p_c6';\r\n-G.Properties.VariableNames{'mean_p_c7_viol_mean'} = 'p_c7';\r\n-G.Properties.VariableNames{'mean_p_c8_viol_mean'} = 'p_c8';\r\n-\r\n-%% Plot helper settings\r\n-metricNames = {'p_c6','p_c7','p_c8'};\r\n-metricLabels = {'C6 violation rate','C7 violation rate','C8 violation rate'};\r\n-scenarioList = {'user_num','f_scale'};\r\n-if strcmpi(plotMode, 'groupedbar')\r\n-    outNames = {'c6_c8_vs_user_num_groupedbar.png','c6_c8_vs_f_scale_groupedbar.png'};\r\n-else\r\n-    outNames = {'c6_c8_vs_user_num.png','c6_c8_vs_f_scale.png'};\r\n+% 提取所需列\r\n+missing = {};\r\n+selectedCols = {};\r\n+for i = 1:length(requiredCols)\r\n+    req = requiredCols{i};\r\n+    if isKey(colMap, req)\r\n+        selectedCols{end+1} = colMap(req);\r\n+    else\r\n+        missing{end+1} = req;\r\n+    end\r\n end\r\n+if ~isempty(missing)\r\n+    error('缺少列: %s\\n实际列名: %s', strjoin(missing, ', '), strjoin(actualCols, ', '));\r\n+end\r\n+T_sel = dataTable(:, selectedCols);\r\n+T_sel.Properties.VariableNames = requiredCols;\r\n \r\n-for s = 1:numel(scenarioList)\r\n-    sc = scenarioList{s};\r\n-    S = G(G.scenario == sc, :);\r\n-    if isempty(S)\r\n-        warning('No rows found for scenario=%s. Skip plotting.', sc);\r\n-        continue;\r\n+% ==================== 通用绘图函数 ====================\r\n+function plot_c6_c8(T_sel, scenarioName, xLabel, outFile, outDir)\r\n+    % 过滤指定场景\r\n+    if isnumeric(T_sel.scenario)\r\n+        T_sel.scenario = string(T_sel.scenario);\r\n     end\r\n+    T_s = T_sel(strcmp(T_sel.scenario, scenarioName), :);\r\n+    if isempty(T_s)\r\n+        error('未找到 scenario = %s 的数据', scenarioName);\r\n+    end\r\n \r\n-    % Sort values and policies for stable visuals\r\n-    xVals = unique(S.value);\r\n-    xVals = sort(xVals);\r\n-    policies = unique(S.policy);\r\n-    policies = sort(policies);\r\n+    % 按 (value, policy) 聚合\r\n+    [G, valueGroup, policyGroup] = findgroups(T_s.value, T_s.policy);\r\n+    c6_mean = splitapply(@mean, T_s.p_c6_viol_mean, G);\r\n+    c7_mean = splitapply(@mean, T_s.p_c7_viol_mean, G);\r\n+    c8_mean = splitapply(@mean, T_s.p_c8_viol_mean, G);\r\n+    agg = table(valueGroup, policyGroup, c6_mean, c7_mean, c8_mean, ...\r\n+        'VariableNames', {'value', 'policy', 'c6', 'c7', 'c8'});\r\n \r\n-    % Style map\r\n-    nP = numel(policies);\r\n-    cmap = lines(max(nP, 7));\r\n-    mk = {'o','s','d','^','v','>','<','p','h','x','+'};\r\n+    % 策略顺序\r\n+    keepPolicies = {'Full', 'IQL', 'No-EVT', 'C6-Only', 'Non-MEC', 'Stand-alone MEC', 'Random', 'Lyapunov-Greedy'};\r\n+    agg = agg(ismember(agg.policy, keepPolicies), :);\r\n+    if isempty(agg)\r\n+        error('没有找到指定的策略数据。请检查策略名称是否与CSV中的一致。');\r\n+    end\r\n+    policies = keepPolicies(ismember(keepPolicies, unique(agg.policy)));\r\n \r\n-    fig = figure('Color','w', 'Position', [100 100 1400 420]);\r\n-    tl = tiledlayout(1, 3, 'Padding', 'compact', 'TileSpacing', 'compact');\r\n-    title(tl, sprintf('Violation Rates under scenario = %s', sc), ...\r\n-        'FontWeight', 'bold', 'FontSize', 13);\r\n+    values = sort(unique(agg.value));\r\n \r\n-    for m = 1:3\r\n-        nexttile; hold on; grid on; box on;\r\n-        yField = metricNames{m};\r\n+    fig = figure('Position', [100 100 1500 460]);\r\n+    tiledlayout(1, 3, 'Padding', 'compact', 'TileSpacing', 'compact');\r\n+    metrics = {'c6', 'c7', 'c8'};\r\n+    titles = {'C6违约率', 'C7违约率', 'C8违约率'};\r\n+    colors = lines(numel(policies));\r\n+    markers = {'o', 's', 'd', '^', 'v', '>', '<', 'p', 'h', 'x', '+'};\r\n \r\n-        if strcmpi(plotMode, 'groupedbar')\r\n-            Y = nan(numel(xVals), nP);\r\n-            for p = 1:nP\r\n-                pol = policies(p);\r\n-                P = S(S.policy == pol, :);\r\n-                for xi = 1:numel(xVals)\r\n-                    r = P(P.value == xVals(xi), :);\r\n-                    if ~isempty(r)\r\n-                        Y(xi, p) = r.(yField)(1);\r\n-                    end\r\n-                end\r\n-            end\r\n-            b = bar(xVals, Y, 'grouped', 'LineStyle', 'none');\r\n-            for p = 1:min(nP, numel(b))\r\n-                b(p).FaceColor = cmap(p,:);\r\n-                b(p).DisplayName = char(policies(p));\r\n-            end\r\n-        else\r\n-            for p = 1:nP\r\n-                pol = policies(p);\r\n-                P = S(S.policy == pol, :);\r\n-                [~, ord] = sort(P.value);\r\n-                Px = P.value(ord);\r\n-                Py = P.(yField)(ord);\r\n-\r\n-                plot(Px, Py, 'LineWidth', 1.8, ...\r\n-                    'Color', cmap(p,:), ...\r\n-                    'Marker', mk{mod(p-1, numel(mk))+1}, ...\r\n-                    'MarkerSize', 6, ...\r\n-                    'DisplayName', char(pol));\r\n-            end\r\n+    for i = 1:3\r\n+        nexttile;\r\n+        hold on;\r\n+        for pIdx = 1:numel(policies)\r\n+            policy = policies{pIdx};\r\n+            subData = agg(strcmp(agg.policy, policy), :);\r\n+            [~, idx] = ismember(values, subData.value);\r\n+            y = nan(size(values));\r\n+            y(idx) = subData.(metrics{i});\r\n+            plot(values, y, 'LineWidth', 1.8, ...\r\n+                'Marker', markers{mod(pIdx-1, numel(markers))+1}, ...\r\n+                'MarkerSize', 5.5, 'Color', colors(pIdx,:), ...\r\n+                'DisplayName', policy);\r\n         end\r\n-\r\n-        xlabel(sc, 'FontSize', 11);\r\n-        ylabel(metricLabels{m}, 'FontSize', 11);\r\n-        ylim([0, 1]);\r\n-        xlim([min(xVals), max(xVals)]);\r\n-        title(metricLabels{m}, 'FontWeight', 'bold');\r\n+        hold off;\r\n+        xlabel(xLabel);\r\n+        ylabel(titles{i});\r\n+        ylim([0 1]);\r\n+        grid on;\r\n     end\r\n \r\n-    lgd = legend('Location','southoutside', 'NumColumns', min(4, nP));\r\n-    lgd.Box = 'off';\r\n+    lgd = legend('Location', 'best', 'NumColumns', 1);\r\n+    lgd.Box = 'on';\r\n \r\n-    outPath = fullfile(outDir, outNames{s});\r\n-    exportgraphics(fig, outPath, 'Resolution', 300);\r\n-    fprintf('Saved: %s\\n', outPath);\r\n+    if ~exist(outDir, 'dir')\r\n+        mkdir(outDir);\r\n+    end\r\n+    outFileFull = fullfile(outDir, outFile);\r\n+    if exist('exportgraphics', 'file')\r\n+        exportgraphics(fig, outFileFull, 'Resolution', 300);\r\n+    else\r\n+        print(fig, outFileFull, '-dpng', '-r300');\r\n+    end\r\n+    fprintf('图片已保存至: %s\\n', outFileFull);\r\n end\r\n \r\n-fprintf('Done.\\n');\r\n+% ==================== 生成图A与图C ====================\r\n+plot_c6_c8(T_sel, 'user_num', '设备数', 'c6_c8_vs_user_num_line.png', outDir);\r\n+plot_c6_c8(T_sel, 'f_scale', '计算资源缩放系数', 'c6_c8_vs_f_scale_line.png', outDir);\r\n"
                }
            ],
            "date": 1775015621931,
            "name": "Commit-0",
            "content": "%% plot_c6_c8_matlab.m\r\n% Plot C6-C8 violation rates for two stress dimensions:\r\n% 1) scenario = user_num\r\n% 2) scenario = f_scale\r\n% Input file expected: new_simulation/results_paper_c5/stress_scan_summary.csv\r\n\r\nclear; clc; close all;\r\n\r\n%% Paths\r\nthisFileDir = fileparts(mfilename('fullpath'));\r\ninputCsv = fullfile(thisFileDir, 'results_paper_c5', 'stress_scan_summary.csv');\r\noutDir = fullfile(thisFileDir, 'figures_paper_c5');\r\n\r\nif ~isfile(inputCsv)\r\n    error('Input CSV not found: %s', inputCsv);\r\nend\r\nif ~isfolder(outDir)\r\n    mkdir(outDir);\r\nend\r\n\r\n%% Load and sanitize table\r\nT = readtable(inputCsv, 'TextType', 'string');\r\n\r\nrequiredCols = [\"scenario\",\"value\",\"policy\", ...\r\n    \"p_c6_viol_mean\",\"p_c7_viol_mean\",\"p_c8_viol_mean\"];\r\nfor i = 1:numel(requiredCols)\r\n    if ~ismember(requiredCols(i), T.Properties.VariableNames)\r\n        error('Missing required column: %s', requiredCols(i));\r\n    end\r\nend\r\n\r\n% Convert numeric-like columns safely\r\nnumCols = [\"value\",\"p_c6_viol_mean\",\"p_c7_viol_mean\",\"p_c8_viol_mean\"];\r\nfor i = 1:numel(numCols)\r\n    c = numCols(i);\r\n    if ~isnumeric(T.(c))\r\n        T.(c) = str2double(string(T.(c)));\r\n    end\r\nend\r\n\r\n% Drop rows with missing key values\r\nvalid = ~ismissing(T.scenario) & ~ismissing(T.policy) & ...\r\n        ~isnan(T.value) & ~isnan(T.p_c6_viol_mean) & ...\r\n        ~isnan(T.p_c7_viol_mean) & ~isnan(T.p_c8_viol_mean);\r\nT = T(valid, :);\r\n\r\n%% Aggregate duplicates by (scenario, value, policy)\r\nG = groupsummary(T, {'scenario','value','policy'}, 'mean', ...\r\n    {'p_c6_viol_mean','p_c7_viol_mean','p_c8_viol_mean'});\r\n\r\n% Rename grouped columns for easier access\r\nG.Properties.VariableNames{'mean_p_c6_viol_mean'} = 'p_c6';\r\nG.Properties.VariableNames{'mean_p_c7_viol_mean'} = 'p_c7';\r\nG.Properties.VariableNames{'mean_p_c8_viol_mean'} = 'p_c8';\r\n\r\n%% Plot helper settings\r\nmetricNames = {'p_c6','p_c7','p_c8'};\r\nmetricLabels = {'C6 violation rate','C7 violation rate','C8 violation rate'};\r\nscenarioList = {'user_num','f_scale'};\r\noutNames = {'c6_c8_vs_user_num.png','c6_c8_vs_f_scale.png'};\r\n\r\nfor s = 1:numel(scenarioList)\r\n    sc = scenarioList{s};\r\n    S = G(G.scenario == sc, :);\r\n    if isempty(S)\r\n        warning('No rows found for scenario=%s. Skip plotting.', sc);\r\n        continue;\r\n    end\r\n\r\n    % Sort values and policies for stable visuals\r\n    xVals = unique(S.value);\r\n    xVals = sort(xVals);\r\n    policies = unique(S.policy);\r\n    policies = sort(policies);\r\n\r\n    % Style map\r\n    nP = numel(policies);\r\n    cmap = lines(max(nP, 7));\r\n    mk = {'o','s','d','^','v','>','<','p','h','x','+'};\r\n\r\n    fig = figure('Color','w', 'Position', [100 100 1400 420]);\r\n    tl = tiledlayout(1, 3, 'Padding', 'compact', 'TileSpacing', 'compact');\r\n    title(tl, sprintf('Violation Rates under scenario = %s', sc), ...\r\n        'FontWeight', 'bold', 'FontSize', 13);\r\n\r\n    for m = 1:3\r\n        nexttile; hold on; grid on; box on;\r\n        yField = metricNames{m};\r\n\r\n        for p = 1:nP\r\n            pol = policies(p);\r\n            P = S(S.policy == pol, :);\r\n            [~, ord] = sort(P.value);\r\n            Px = P.value(ord);\r\n            Py = P.(yField)(ord);\r\n\r\n            plot(Px, Py, 'LineWidth', 1.8, ...\r\n                'Color', cmap(p,:), ...\r\n                'Marker', mk{mod(p-1, numel(mk))+1}, ...\r\n                'MarkerSize', 6, ...\r\n                'DisplayName', char(pol));\r\n        end\r\n\r\n        xlabel(sc, 'FontSize', 11);\r\n        ylabel(metricLabels{m}, 'FontSize', 11);\r\n        ylim([0, 1]);\r\n        xlim([min(xVals), max(xVals)]);\r\n        title(metricLabels{m}, 'FontWeight', 'bold');\r\n    end\r\n\r\n    lgd = legend('Location','southoutside', 'NumColumns', min(4, nP));\r\n    lgd.Box = 'off';\r\n\r\n    outPath = fullfile(outDir, outNames{s});\r\n    exportgraphics(fig, outPath, 'Resolution', 300);\r\n    fprintf('Saved: %s\\n', outPath);\r\nend\r\n\r\nfprintf('Done.\\n');\r\n"
        }
    ]
}